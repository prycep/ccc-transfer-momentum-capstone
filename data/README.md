# Data directories

- `raw_gz/`: original college-level `.csv.gz` files exported from DataVista.
- `processed/`: analysis-ready college × cohort-year panel generated by `analysis.py`.

Raw DataVista files are public but are intentionally excluded from version control because of their size. See the repository README for exact export settings.

